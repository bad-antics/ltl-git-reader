// examples/demo.ltl — runnable demo for ltl_git_reader

import std::io
use ltl_git_reader as lib

fn main() {
    io::println("ltl_git_reader demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}