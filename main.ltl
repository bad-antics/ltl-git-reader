// ltl-git-reader/main.ltl
//
// Read-only Git object-store reader (loose + packfile).
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-git-reader v0.1.0")
    io::println("Read-only Git object-store reader (loose + packfile).")
    0
}
